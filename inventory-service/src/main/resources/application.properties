spring.application.name=inventory-service
server.port=${SERVER_PORT:8092}

# --- MongoDB ---
# spring.data.mongodb.uri is deprecated at "error" level since Boot 4.0, meaning
# it is no longer bound: setting it silently fell back to localhost:27017.
spring.mongodb.uri=${MONGO_URI:mongodb://localhost:27018/orderflow}
spring.mongodb.uuid-representation=standard

# --- Kafka ---
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=inventory-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
# --- gRPC Server ---
grpc.server.port=9095

# --- Logging ---
logging.level.root=INFO
logging.level.com.pmfml.orderflow=DEBUG
